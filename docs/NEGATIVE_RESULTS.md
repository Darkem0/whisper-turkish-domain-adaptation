# Negative results are first-class

The synthetic fixture intentionally shows that an adapter candidate can reduce an error for one domain while introducing a repetition error for another. This is a generated teaching case, not a report of historical performance.

Preserve negative results with the same manifest, metric definition, and split metadata as positive results. A model should not be described as better overall merely because it improves a preferred subset.
