# A6_v2 VRAM feasibility

`PASSED` for the two-step smoke only. Peak CUDA allocated was 1,836.20 MiB, peak CUDA reserved was 2,044.00 MiB, peak driver VRAM was 3,251 MiB, and process RSS was 1,604.34 MiB. The configured smoke gate is 10,000 MiB reserved.

This is not evidence for a completed 200-step run; it only removes the immediate two-step resource blocker.
