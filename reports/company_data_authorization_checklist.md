# Company data authorization checklist

All fields in `contracts/company_data_authorization_template.yaml` must be completed by the data owner/authorized role before any company-data materialization. Evaluation must be explicitly allowed; external upload must remain forbidden. Training and pseudo-labeling require separate explicit decisions and are not implied by evaluation authorization.
